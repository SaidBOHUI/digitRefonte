services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: ${DATABASE_URL}
      MODEL_PATH: model/model.h5
      CORS_ORIGINS: '["https://digiteye.sbohui.fr"]'

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_URL: https://api.sbohui.fr/api
    ports:
      - "80:80"
